// Task: Rewrite a function multiplication as an arrow function. Test the arrow function with different contexts.
//Task: Rewrite a function sum as an arrow function. Test the arrow function with different contexts.
//Task: Rewrite a function division as an arrow function. Test the arrow function with different contexts.

const multiplication = (a, b) => a * b;
  
  
  const sum = (a, b) => a + b;

  
  const division = (a, b) => a / b;
  
  console.log(multiplication(2, 3));
  console.log(sum(2, 3));
  console.log(division(2, 3));
  

